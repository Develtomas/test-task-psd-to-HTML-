+ максимально подогнать по макету. ограничить максимальную ширину до 940px
(ширина всех блоков ограничена max-widht 940px, так же у навигационной панели задана min-height 60px. Блок новостей, регистрации и "некий блок" в высоте не ограничивал, так как они могут содержать контент)

+ из стилей убрать id-шники. в css файлах должны в качестве селектором использоваться только стили (.name_class)
(переведено в стили)

+ убрать <br> отступ перед шапкой, сделать его margin'ом
(убран через margin-top)

+ отказаться от inline-стилей
(ушли в классы)

+ использовать изображение-спрайт, кнопку гамбургер подправить, убрать лишнее изображение, добавить cursor:pointer
(pointer добавил, спрайт изменил , добавил другие элементы, лишнее убрал)

+ стилизовать чекбокс, желательно css
(только через css)

+ на мобильнике ползет меню в футере (при увеличение количества пунктов), не закрытый li в футере
(li закрыт, футер переделал - теперь не ползет при добавлении строк , задавать min-height строго не стал, чтобы корректно работало в мобильной версии.)

+ у заголовков добавлены классы: redd ptsa-nw-23 - не очень удобно задавать отдельный класс для цвета и фонтсайза, добавить общие стили для заголовков
(для заголовков исправлено, так же для части текста. Много дополнительных стилей оставлено , так как макет изобилует разными размерами шришта + разные начертания)

+ у блока Вход через соц сети задана высота
(высоту убрал)

+- у конкретной новости: display: table-cell и width: 10000px, используете подсетку будстрап
(сетку использовать не стал , так как все элементы новостей находятся в одном div -то есть нужно было создавать еще массу сложенных 3 вложенных div , чтобы элементы располагались по 2 на строке. Table-cell оставил - за счет них ровнение по вертикали идет , width убрал. Задал максимальные высоты блоков с текстом, чтобы лишнее обрезалось)

-+ разделитель блока новостей и авторизации лишний блок, используйте сетку бутстрап
(данный блок пришлось оставить, он вмонтирован в сетку бутстрапа (1/12 левого блока) если задавать разделить между блоками блоками как solid-border то это криво выглядит в мобильной версии, поэтому блок оставил, под него подогнал разделители между другими элементами - 26.1px)

-+ не скругленные углы внизу
(сделал "тень" у всех блоков с помощью border. Как делать задвоенные скругления не знаю, футер по макету без скруглений)

-+ кнопки соц сетей сделаны сложно, разметка для них может быть проще (меньше вложенности и тегов)
(в целом работают, позже постараюсь сделать варианты проще - этот сделан по бутстрэповским примерам)

+ иконка крестика не соответствует макету
(исправлена на спрайт с макета)

+Подвал по-хорошему должен прилипать к низу страницы
(не переделывал, что-то исправил, видимо margin убрал, само решилось)

-+ В стилях встретила !important в нескольких местах, можно было обойтись без ниx
(пробовал поменять не очень успешно, оставил - как наиболее простое решение)

+ В блоке новостей заголовки логично было бы сделать тоже ссылками, как и картинки
(изменены , сделан hover)

+У кнопок убрать outline
(убрал , но с использованием !important)

+ на планшетах нет поиска 
(исправлено корректировкой @media (min-width: 768px).navbar-form {width} со значения авто на значение 25%)

+ не стилизованный селект
(второй селект, появляющийся при сжатии не стилизовался- этого в условиях нет. Первый стилизован. Расположение search bar на навигационой панели не соответствует макету , для того ,чтобы search-bar корректно выравнивался по ширине на мобильных устройствах.)
__________________________________________________________________________________

+ выровнять текст измененных блоков

+ проблемы с фонт колор в кнопках (исправлено через bootstrap.css)

+ гугл кнопка не заменил рисунок (кнопка с awesome лучше)

+ размер кнопок
(кнопки регистрации оставил, остальные подогнал по макету)